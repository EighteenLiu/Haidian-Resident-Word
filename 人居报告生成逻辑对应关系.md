# 海淀区农村人居环境报告生成逻辑对应关系

本文档基于 `data/2.人居报告` 中的 3 个原始 xlsx 文件，以及 `data/2.人居报告/提交格式` 中的 1 个提交 xlsx 和 1 个提交 docx 样例整理。当前阶段只梳理生成逻辑，不修改现有提交样例和模板文件。

## 1. 文件角色

### 1.1 原始数据

| 文件 | 作用 | 主要字段/工作表 |
| --- | --- | --- |
| `0.人居村及指标类别.xlsx` | 基础字典。提供纳入农村人居环境检查的镇村清单，以及检查小类到人居报告口径大类、次大类的映射。 | `人居村`：街镇名称、人居村；`人居指标`：大类名称、次大类、小类名称 |
| `0.农村人居道路202604--农业农村局道路台账-删除聂各庄东路.xlsx` | 人居道路台账。当前口径以该文件提供的材料为准，用于识别现场检查数据中“涉及人居的道路”。筛选时重点核对 `街镇分中心`、`检查点位（3级）` 和 `道路`/道路名称。 | 所属街道、小区（村）名称、道路、备注-别名、村+道路拼接辅助列 |
| `1.6月现场检查用数据.xlsx` | 主数据源。提交 xlsx 的有效明细和 Word 报告统计均来自该文件的明细工作表。 | `数据`：月份、备注、编号、案件来源、案件状态、案件分类、检查点位、检查指标、街镇分中心、整改/解决字段等 |

### 1.2 目标提交文件

| 文件 | 作用 | 来源 |
| --- | --- | --- |
| `附件3：海淀区农村人居环境检查情况统计表-城环建平台（6月）(1).xlsx` | 提交用统计表参考样例。当前自动化只统计问题数量，不计算扣分、最终得分和合格情况。 | 样例含 663 条明细；按当前辅助拼接列匹配口径，6 月纳入 664 条有效案件。 |
| `海淀区2026年6月份农村人居环境检查分析报告(1).docx` | 提交用分析报告参考样例。 | 正文数字和 4 张表来自同一批有效案件；当前辅助拼接列匹配口径下 6 月为 664 条。 |

## 2. 有效数据筛选口径

提交 xlsx 的 `数据明细` 工作表第 1 行为表头，第 2 行作为明细样式模板行保留。生成时会清空第 2 行旧值、删除第 3 行以后的旧明细，再从第 2 行开始写入有效案件；后续新增行复制第 2 行格式。

从提交明细编号反查主数据源可确认：参考样例 663 条均来自 `1.6月现场检查用数据.xlsx` 的 `数据` 工作表。当前辅助拼接列匹配口径下，6 月最终纳入 664 条，且具有以下共同特征：

| 源字段 | 口径 |
| --- | --- |
| `月份` | 当前报告月份，例如 6 月报告取 `6月` |
| `检查点位（1级）` | `北部地区` |
| `上报扣分案件` | `是` |
| `案件状态` | 纳入明细均为 `结案` |
| `解决案件库` | 纳入明细均为 `是`，因此 Word 正文写“解决率100%” |
| `检查点位（3级）` | 必须能匹配到 24 个人居村字典 |
| `检查指标（3级）` | 必须能匹配到人居指标字典 |
| 道路台账 | 统计纳入按道路匹配规则执行，未纳入记录不写入正式统计表；程序只在运行日志中记录道路匹配数量 |

备注：参考样例中 `备注` 字段为 `现场检查` 661 条、`视频监控` 2 条，但 Word 标题和正文仍按“现场检查”专项报告表述，说明报告统计口径不是仅筛 `备注=现场检查`。

### 2.1 人居道路识别口径

用户已确认：道路台账只看当前提供材料里的 `0.农村人居道路202604--农业农村局道路台账-删除聂各庄东路.xlsx`。从现场检查数据筛选涉及人居的道路时，重点看以下字段：

| 现场检查数据字段 | 用途 |
| --- | --- |
| `街镇分中心` | 与道路台账中的 `所属街道` 对应，用于限定镇街范围。 |
| `检查点位（3级）` | 与道路台账中的 `小区（村）名称` 对应，用于限定村。 |
| `道路`/道路名称 | 与 `检查点位（3级）` 拼接后，匹配道路台账 `备注-别名` 后一列的辅助拼接值。无括号时使用道路原值；带括号时同时使用括号前名称、括号内名称和带括号完整道路名。 |

程序实现口径：先按报告月份和有效案件口径筛出候选数据，再用道路台账做道路匹配。源道路名无括号时生成 `检查点位（3级）+道路原值`；源道路名带括号时生成 `检查点位（3级）+括号前名称`、`检查点位（3级）+括号内名称` 和 `检查点位（3级）+带括号完整道路名` 三个候选。任意候选命中道路台账 `备注-别名` 后一列的辅助拼接值，即纳入统计。道路匹配结果只用于决定是否纳入正式统计，并在运行日志中输出各状态数量，不再生成单独的道路匹配校验 xlsx。

当前统计纳入规则为：道路拼接候选命中道路台账辅助拼接列的案件纳入；道路为空且不是 `考评上报` 的案件纳入；其他未命中道路台账的案件不纳入统计。

## 3. 提交 xlsx 生成逻辑

### 3.1 工作表结构

目标 xlsx 有 3 个工作表：

| 工作表 | 生成逻辑 |
| --- | --- |
| `sheet1` | 村级问题数量统计表。按 24 个村逐行统计各问题指标数量；当前不计算扣分、最终得分和合格情况。 |
| `数据明细` | 从主数据源 `数据` 工作表筛选有效案件后复制指定列生成；保留模板第 2 行格式，新写入明细从第 2 行开始，后续行复制第 2 行样式。用户可在更新统计表并生成报告前人工修改该表。 |
| `Sheet2` | 样例中的辅助得分表。当前自动化不计算得分，该页可保留格式但不作为核心输出逻辑。 |

### 3.2 `数据明细` 字段映射

目标 `数据明细` 从源 `数据` 复制大部分字段，但删除了 5 个源字段：

| 源 `数据` 字段 | 目标 `数据明细` |
| --- | --- |
| `月份` | 不输出 |
| `备注` | 不输出 |
| `编号` 至 `公园` | 原样输出 |
| `检查点位（1级）` 至 `街镇分中心` | 原样输出 |
| `序列` | 不输出 |
| `区委办局` 至 `整改责任单位` | 原样输出 |
| `整改责任单位-报告` | 不输出 |
| `处置部门名称（一级部门）` 至 `超期未整改` | 原样输出 |

因此源表 42 列变为目标明细 37 列。

### 3.3 `sheet1` 村级行

村级行来自 `0.人居村及指标类别.xlsx` 的 `人居村` 工作表，共 24 个村：

| 镇 | 村 |
| --- | --- |
| 苏家坨镇 | 柳林村、七王坟村、车耳营村、台头村、梁家园村、西埠头村、聂各庄村、草厂村 |
| 上庄镇 | 西辛力屯村、常乐村、双塔村、东小营村、梅所屯村、后章村、永泰庄村、白水洼村、北玉河村、前章村、李家坟村、罗家坟村、西闸村、八家村、皂甲屯村、东马坊村 |

输出显示时，样例把村名末尾的“村”去掉，例如 `柳林村` 输出为 `柳林`。

### 3.4 指标列映射

`sheet1` 的 J:AB 列是人居指标统计列。每个村、每个指标列的值等于有效案件中：

`检查点位（2级）=镇` 且 `检查点位（3级）=村` 且 `检查指标（3级）属于该列小类` 的案件数。

样例中的指标列如下：

| 列 | 大类 | 次大类 | 对应源 `检查指标（3级）` |
| --- | --- | --- | --- |
| J | 农村厕所革命 | 公厕未按规定正常开放 | 公厕未按要求时间开放 |
| K | 农村厕所革命 | 公厕厕位有污物、脏臭 | 厕内脏污 |
| L | 农村厕所革命 | 公厕设施损坏不能正常使用或制度未上墙 | 厕内设施损坏 |
| M | 农村生活垃圾治理 | 垃圾收集设施存在满冒、渗漏、污渍、破损或周边管护不到位问题 | 垃圾桶破损、脏污、满冒；垃圾桶周边不洁 |
| N | 农村生活垃圾治理 | 垃圾分类不规范 | 垃圾混放 |
| O | 农村生活垃圾治理 | 村域内有较为严重的暴露垃圾和积存生活垃圾 | 垃圾乱倒乱卸 |
| P | 农村生活垃圾治理 | 建筑垃圾未得到规范堆放和处理 | 装修垃圾规范管理；积存垃圾 |
| Q | 农村生活垃圾治理 | 存在清扫保洁不到位、垃圾乱倒乱扔、卫生死角盲区 | 清扫保洁不到位 |
| R | 农村生活污水 | 生活污水直排溢流路面、乱排乱放 | 路面积水、污水 |
| S | 农村生活污水 | 沟渠有垃圾、杂物淤积堵塞影响排水或有明显黑臭水体 | 沟渠不洁 |
| T | 村容村貌整治 | 侵街占道私搭乱建 | 新生违建 |
| U | 村容村貌整治 | 乱堆乱放 | 堆物堆料 |
| V | 村容村貌整治 | 乱贴乱画乱挂 | 非法小广告 |
| W | 村容村貌整治 | 村内道路破损失修、坑洼积水等影响群众出行 | 路面破损；路面塌陷 |
| X | 村容村貌整治 | 公共绿化存在明显枯死、破坏、占绿毁绿 | 侵占损毁绿地 |
| Y | 村容村貌整治 | 村内架空线杂乱无序交织，存在安全隐患 | 架空线凌乱；架空线垂落 |
| Z | 村容村貌整治 | 路灯损毁、养护不到位、不亮现象 | 路灯设施破损 |
| AA | 村容村貌整治 | 田园整洁干净 | 田园整洁干净 |
| AB | 村容村貌整治 | 残垣断壁整治 | 外立面破损、脏污 |

当前辅助拼接列匹配口径下，6 月有效数据实际出现的 `检查指标（3级）` 及总数为：

| 指标小类 | 数量 |
| --- | ---: |
| 清扫保洁不到位 | 292 |
| 非法小广告 | 124 |
| 堆物堆料 | 122 |
| 积存垃圾 | 46 |
| 路面破损 | 37 |
| 沟渠不洁 | 19 |
| 路灯设施破损 | 11 |
| 架空线垂落 | 5 |
| 侵占损毁绿地 | 3 |
| 路面积水、污水 | 2 |
| 架空线凌乱 | 2 |
| 厕内脏污 | 1 |

合计 664。

### 3.5 数量统计口径

用户已确认：当前不需要计算分数，只统计问题数量。因此后续生成时不需要处理村庄常驻人口系数、市级垃圾分类示范小区台账、严重/普通问题扣分、最终得分和合格情况。

`sheet1` 的核心输出为各村在 J:AB 各指标列的问题数：

1. 按 `0.人居村及指标类别.xlsx` 的 `人居村` 工作表生成 24 个村的固定行。
2. 按有效案件中的 `检查点位（2级）`、`检查点位（3级）` 定位镇村。
3. 按 `检查指标（3级）` 映射到 J:AB 的具体指标列。
4. 对每个村、每个指标列计数。
5. `D`、`E`、`F`、`G`、`H` 等评分相关列如需保留格式，可置空、保留样例标题或标注“不计算”；不能再沿用样例公式自动得分，避免旧评分逻辑继续生效。

检查轮次已确认固定为每月 2 次。如需要展示“每轮平均问题数”，统一按 `问题总数 / 2` 计算；该值只用于报告表述或数量均值，不用于扣分。

## 4. Word 分析报告生成逻辑

Word 报告不再直接使用台账生成阶段的内存统计，而是从所选台账 xlsx 的 `数据明细` 重新读取案件并统计。点击 `更新统计表并生成报告` 后，程序先按当前 `数据明细` 同步更新台账 `sheet1`，再渲染 Word。用户在台账生成后对 `数据明细` 的增删改，会体现在 `sheet1` 和 Word 报告中。当前辅助拼接列匹配口径下，6 月为 664 条。标题月份来自报告月份，例如：

`海淀区2026年6月农村人居环境现场检查数据分析报告`

### 4.1 开头和总体情况

| 文本位置 | 生成逻辑 |
| --- | --- |
| `2026年6月` | 从报告年月生成 |
| `苏家坨和上庄2镇24个村` | 来自有效案件涉及镇村，或来自 `人居村` 字典 |
| `检查2轮次` | 用户已确认每月固定检查 2 次 |
| `发现农村人居环境问题664个` | 有效案件总数 |
| `每轮平均问题332个` | `round(664 / 2)`，样例为四舍五入到整数 |
| `解决率100%` | 有效案件中 `解决案件库=是` 的比例 |

### 4.2 表 1：问题大类统计

表 1 按 `0.人居村及指标类别.xlsx` 的 `人居指标` 映射，把有效案件的 `检查指标（3级）` 归并到人居大类，并按问题总数从高到低排序：

| 问题类型 | 数量 | 占比 |
| --- | ---: | ---: |
| 农村生活垃圾治理 | 338 | 50.90% |
| 村容村貌整治 | 304 | 45.78% |
| 农村生活污水 | 21 | 3.16% |
| 农村厕所革命 | 1 | 0.15% |
| 总计 | 664 | 100.00% |

Word 渲染后会对表 1 最后一行做后处理：仅当前两个单元格文本均为 `总计` 时，横向合并前两列并居中，避免误合并其他表格或普通数据行。

正文“问题项目分析”的 4 个小段也来自该表，并补充各大类下问题较多村庄。村庄摘要按标准报告写法生成：大类段落使用 `涉及...等N个村` 或 `具体发生在...`；同一镇连续出现时，后续村名省略镇名；并列同数量村使用 `A村和B村（各N个）`。

### 4.3 表 2：具体高发问题

表 2 按具体小类统计，表格按人居问题大类顺序分组，且大类顺序与小类顺序都按数量从高到低展示，最后一行输出 `总计、总计、总计、664、100.00%`；渲染后第二列连续相同的问题大类需要纵向合并，最后一行前三个 `总计` 需要横向合并，合并后的单元格均需上下左右居中。正文“具体高发问题”仍按全局数量列出前三项高发问题：

| 具体问题 | 数量 | 占比 |
| --- | ---: | ---: |
| 清扫保洁不到位 | 292 | 43.98% |
| 非法小广告 | 124 | 18.67% |
| 堆物堆料 | 122 | 18.37% |

前三项合计 538 个，占 `538 / 664 = 81.02%`，对应正文“高发问题依次为清扫保洁不到位、非法小广告、堆物堆料，共538个，占问题总量的81.02%。”

高发问题正文的村庄摘要取前 2 个村，使用 `主要集中在...等N个村`；高发问题名称之间使用顿号连接，不在最后一项前使用 `和`。

表 2 还应包含其余问题小类：积存垃圾、路面破损、沟渠不洁、路灯设施破损、架空线垂落、侵占损毁绿地、路面积水/污水、架空线凌乱、厕内脏污等。

### 4.4 表 3：各镇问题情况

表 3 按有效案件的 `检查点位（2级）` 汇总：

| 镇 | 数量 | 占比 |
| --- | ---: | ---: |
| 苏家坨镇 | 155 | 23.34% |
| 上庄镇 | 509 | 76.66% |

正文同步生成：“苏家坨镇发现问题155个，占比23.34%，上庄镇发现问题509个，占比76.66%。”

Word 渲染后会按标准文件口径把表 3 标题修正为 `农村人居环境检查发现各村问题数量统计表`，并统一为方正小标宋简体、四号、居中；表 3 不输出总计行，不修改原始模板文件。

### 4.5 表 4：各村问题情况

表 4 按 `检查点位（2级）`、`检查点位（3级）` 和人居大类交叉统计，且每个镇内按村问题总数从高到低排序。渲染时两个大类列按提交样例的展示口径写入，列为：

`镇、村、农村生活垃圾治理、村容村貌、生活污水治理、厕所革命、总计`

表 4 最后一行输出 `总计、总计、338、304、21、1、664`，渲染后前两个 `总计` 横向合并并上下左右居中。

样例中表 4 按镇分组，并在镇内按总计由高到低排序。正文“各村问题情况”抽取各镇问题较多村：

| 镇 | 样例正文口径 |
| --- | --- |
| 苏家坨镇 | 问题较多的村为西埠头村30个，台头村和柳林村各22个 |
| 上庄镇 | 问题较多的村为皂甲屯村58个、前章村56个 |

## 5. 后续模板建议

如果进入下一步生成模板，建议新增两个模板文件，不直接覆盖提交样例：

| 模板文件名 | 建议来源 |
| --- | --- |
| `附件3：海淀区农村人居环境检查情况统计表模板.xlsx` | 复制提交 xlsx 的格式、合并单元格和指标列框架，把月份、村级指标计数作为程序填充值；评分相关列不参与计算。 |
| `海淀区2026年x月份农村人居环境检查分析报告模板.docx` | 复制提交 docx 的段落、标题和表格格式，把年月、总数、占比、排名村庄、4 张表改为 Jinja/docxtpl 占位。 |

模板设计时建议把以下内容做成显式配置：

1. 报告年月和筛选月份。
2. 有效数据筛选条件：报告月份、检查点位、是否扣分、是否仅结案、人居道路台账匹配等。
3. 村庄清单及显示名。
4. 指标小类到人居大类/次大类/统计列的映射。
5. 检查轮次，固定为 2。

## 6. 已确认口径和仍需注意

1. 道路台账以当前提供的 `0.农村人居道路202604--农业农村局道路台账-删除聂各庄东路.xlsx` 为准，用于识别涉及人居的道路。筛选现场检查数据时重点看 `街镇分中心`、`检查点位（3级）`、`道路`/道路名称。
2. 当前不计算分数，只统计问题数量。因此村庄常驻人口系数、市级垃圾分类示范小区台账、严重问题扣分、最终得分、合格情况均不进入本阶段逻辑。
3. 每月检查轮次固定为 2 次；报告中的每轮平均问题数按 `问题总数 / 2` 计算。
4. 提交 xlsx `数据明细` 只输出有效案件明细，但模板第 2 行作为样式来源保留；生成时清空第 2 行旧值并复用其格式。报告生成时以用户选择的台账 xlsx 中 `数据明细` 为准重新统计，并先回写同步 `sheet1`。
5. 仍需在程序实现时注意：道路名称可能为空、带括号或与台账写法不完全一致。程序在台账生成日志中输出道路匹配统计，未命中道路台账辅助拼接列的记录不纳入正式统计，但不再额外生成道路匹配校验清单。
6. GUI 分为 `生成台账` 和 `生成报告` 两个模块。`生成台账` 模块负责上传源数据、识别文件、生成 xlsx；生成后可点击 `应用于报告生成` 把台账路径带入报告模块，给用户留下人工修改台账的机会。`生成报告` 模块支持基于附件3 自动识别报告年月和日期范围；按钮为 `更新统计表并生成报告`，先从所选台账 xlsx 的 `数据明细` 重新统计并同步 `sheet1`，再渲染 Word。
7. 打包为 exe 后，以 exe 所在目录作为程序根目录；`config`、`input`、`output`、`.runtime` 均从 exe 同级目录定位，避免冻结程序把配置、模板或临时文件定位到解包目录或 C 盘。release/交付目录除 exe、`config`、`input` 外，还应放入 `README.md` 和 `人居报告生成逻辑对应关系.md` 等必要说明文件。
