使用须知

1. 先在“生成台账”页添加主数据、村指标字典、道路台账。
2. 点击“识别”后再生成台账，确认报告年月和日期范围。
3. 台账生成后，可先修改 `数据明细` 或 `sheet1`，再点“应用于报告生成”。
4. 在“生成报告”页如未自动识别日期，可直接选台账文件，程序会基于附件3重新识别。
5. 交付目录请保留 `config/`、`input/`、`README.md`、`人居报告生成逻辑对应关系.md` 和本文件。
6. 未明确要求时，不生成压缩包。
